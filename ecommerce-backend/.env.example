# Server Configuration
API_PORT=8080

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=ecommerce_db
DB_PORT=5432
DB_SSLMODE=disable

# Redis Configuration
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Application Secret
JWT_SECRET=a-very-strong-and-secret-key

# n8n Webhook URL for product sync
N8N_WEBHOOK_URL=http://localhost:5678/webhook/sync-product-to-directus